# Web

## Setup
1. `cd apps/web`
2. Copy `.env.example` to `.env`.
3. Install deps: `npm install`
4. Start: `npm run dev`

`NEXT_PUBLIC_API_URL` should point to the API server.

## Logo
Place the Genesis Community logo at `apps/web/public/genesis-logo.png`.
