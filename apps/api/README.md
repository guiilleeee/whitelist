# API

## Setup
1. `cd apps/api`
2. Copy `.env.example` to `.env` and fill values.
3. Install deps: `npm install`
4. Start: `npm run dev`

## Environment notes
- `DISCORD_REDIRECT_URI` must match your Discord app OAuth2 settings.
- `ADMIN_DISCORD_IDS` is a comma-separated list of Discord user IDs.
